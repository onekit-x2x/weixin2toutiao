import onekit from "../../onekit/lib/onekit";
/*
export default class MapContext {
    constructor(){
        this.id = onekit.guid();
    }
    getCenterLocation(object){
        var mapContext = {
            id:this.id,
            method : 'getCenterLocation'
        }
        getApp().onekit.context.send(mapContext, object);
        
    }
    getRegion(object){
        var mapContext = {
            id:this.id,
            method : 'getRegion'
        };
         getApp().onekit.context.send(mapContext, object);
    }
    getRotate(object){
        var mapContext = {
            id:this.id,
            method : 'getRotate'
        };
        getApp().onekit.context.send(mapContext, object);

    }
    getScale(object){
        var mapContext = {
            id:this.id,
            method : 'getScale'
        };
         getApp().onekit.context.send(mapContext, object);
    }
    getSkew(object){
        var mapContext = {
            id:this.id,
            method : 'getSkew'
        };
         getApp().onekit.context.send(mapContext, object);
    }
    includePoints(object){
        var mapContext = {
            id:this.id,
            method : 'includePoints',
            data : {
                points : oject.points,
                padding : object.padding
            }
        };
         getApp().onekit.context.send(mapContext, object);
    }
    moveToLocation(object){
        var mapContext = {
            id:this.id,
            method : 'moveToLocation',
            data : {
                longitude : oject.longitude,
                latitude : object.latitude
            }
        };
         getApp().onekit.context.send(mapContext, object);
    }
    setCenterOffset(object){
        var mapContext = {
            id:this.id,
            method : 'setCenterOffset',
            data : {
                offset : oject.offset
            }
        };
         getApp().onekit.context.send(mapContext, object);
        
    }
    translateMarker(object){
        var mapContext = {
            id:this.id,
            method : 'setCenterOffset',
            data : {
                offset : oject.offset
            }
        };
         getApp().onekit.context.send(mapContext, object);
    }

}*/
