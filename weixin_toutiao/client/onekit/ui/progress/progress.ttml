<progress 
percent="{{percent}}"
show-info="{{showInfo}}"
stroke-width="{{strokeWidth}}"
color="{{color}}"
active-color="{{activeColor}}"
background-color="{{backgroundColor}}"
active="{{active}}"
>
<slot/>
</progress>
